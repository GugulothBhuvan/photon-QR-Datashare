# Assets

Static application assets: icons, splash imagery and fonts.

App icon and splash configuration are intentionally absent from `app.json`
until branding assets exist, so that builds do not reference missing files.
