/**
 * Global Jest setup.
 *
 * `@testing-library/react-native` registers its matchers automatically, so no
 * matcher import is required here. Keep this file free of protocol or
 * application logic — it configures the test runtime only.
 */

export {};
