/**
 * storage/ — Adapter layer
 *
 * Owns: Filesystem and key-value storage adapters. The only place platform storage APIs appear.
 *
 * May depend on:
 *   - Platform storage APIs
 *   - Utilities (@utils/*)
 *
 * Must NOT depend on:
 *   - Business logic
 *   - Core protocol
 *   - Repositories
 *   - UI
 *
 * Authority: planning/DEPENDENCIES.md and docs/ARCHITECTURE.md.
 * Placeholder barrel — populated from Phase 1 onwards.
 */

export {};
