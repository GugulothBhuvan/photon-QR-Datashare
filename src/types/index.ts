/**
 * types/ — Domain layer
 *
 * Owns: Immutable domain model types and shared type declarations. Types only, no runtime code.
 *
 * May depend on:
 *   - Nothing
 *
 * Must NOT depend on:
 *   - Every other module
 *
 * Authority: planning/DEPENDENCIES.md and docs/ARCHITECTURE.md.
 * Placeholder barrel — populated from Phase 1 onwards.
 */

export {};
