/**
 * screens/ — UI layer
 *
 * Owns: Screen-level composition rendered by the Expo Router routes in app/.
 *
 * May depend on:
 *   - Components (@components/*)
 *   - Hooks (@hooks/*)
 *   - Controllers (@controllers/*)
 *   - State (@state/*)
 *
 * Must NOT depend on:
 *   - Protocol logic
 *   - Packet layer
 *   - QR encoder
 *   - Storage
 *   - Camera implementation
 *
 * Authority: planning/DEPENDENCIES.md and docs/ARCHITECTURE.md.
 * Placeholder barrel — populated from Phase 1 onwards.
 */

export {};
