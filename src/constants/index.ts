/**
 * constants/ — Shared layer
 *
 * Owns: Compile-time constants shared across layers. No behaviour.
 *
 * May depend on:
 *   - Nothing
 *
 * Must NOT depend on:
 *   - Every other module
 *
 * Authority: planning/DEPENDENCIES.md and docs/ARCHITECTURE.md.
 * Placeholder barrel — populated from Phase 1 onwards.
 */

export {};
