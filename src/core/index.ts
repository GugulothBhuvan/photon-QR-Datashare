/**
 * core/ — Core Protocol layer
 *
 * Owns: Protocol engine: session, manifest, packet and recovery logic. Pure, deterministic, platform independent.
 *
 * May depend on:
 *   - Domain models (@domain/*)
 *   - Utilities (@utils/*)
 *   - Constants (@constants/*)
 *
 * Must NOT depend on:
 *   - React / React Native
 *   - Expo APIs
 *   - UI
 *   - Camera
 *   - Storage implementations
 *
 * Authority: planning/DEPENDENCIES.md and docs/ARCHITECTURE.md.
 * Placeholder barrel — populated from Phase 1 onwards.
 */

export {};
