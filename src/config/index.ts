/**
 * config/ — Shared layer
 *
 * Owns: Runtime configuration and the dependency-injection container composition root.
 *
 * May depend on:
 *   - Any layer (composition root)
 *   - Constants (@constants/*)
 *
 * Must NOT depend on:
 *   - Being imported by the core protocol
 *
 * Authority: planning/DEPENDENCIES.md and docs/ARCHITECTURE.md.
 * Placeholder barrel — populated from Phase 1 onwards.
 */

export {};
