/**
 * workers/ — Service layer
 *
 * Owns: Off-main-thread execution for expensive protocol and encoding work.
 *
 * May depend on:
 *   - Core protocol (@core/*)
 *   - Utilities (@utils/*)
 *
 * Must NOT depend on:
 *   - UI
 *   - Navigation
 *   - Controllers
 *
 * Authority: planning/DEPENDENCIES.md and docs/ARCHITECTURE.md.
 * Placeholder barrel — populated from Phase 1 onwards.
 */

export {};
